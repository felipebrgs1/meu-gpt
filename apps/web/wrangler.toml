name = "meu-gpt-web"
compatibility_date = "2024-09-25"

# Static-only Worker: serve o build do Vite (apps/web/dist).
# Deploy: pnpm --filter @meu-gpt/web build && wrangler deploy
# (rode dentro de apps/web — o directory é relativo a este arquivo)
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
